{
  "routes": [
    {
      "method": "POST",
      "path": "/send",
      "handler": "send-sms.send",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/schedule",
      "handler": "send-sms.schedule",
      "config": {
        "policies": []
      }
    }
  ]
}
